<h1 class="messages-title d-none d-md-block">Messages</h1>

<div class="row m-0 chat-wrap">
  <!-- conversations -->
  <div class="col-12 col-lg-auto h-100 d-none d-lg-block" id="conversationColumn">
    <!-- search bar -->
      <%= form_for_filterrific @filterrific,
          html: { id: 'filterrific-no-ajax-auto-submit' }, url: messages_path do |f| %>
      <div class="form-group">
        <%= f.text_field(
          :search_query,
          class: 'filterrific-periodically-observed form-control',
          placeholder: 'Search'
        ) %>
      </div>
    <% end %>
    
    <!-- conversations -->
    <%= render(
      partial: 'chats/list',
      locals: { users: @users }
    ) %>
  </div>
  <div class="col pl-0 pr-3 h-100 d-lg-block" id="chatColumn">
    <%= render 'chatting' %>
  </div>
</div>
