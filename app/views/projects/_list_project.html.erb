<div id="projects-container" class="mb-5 my-3 p-3 border rounded position-relative">
  <div>
    <% if @projects.blank? %>
          <div class="gridProfileActivity">
            <div class="titleactivityProfile">
              <span class="titleYetAdd">Nothing added yet</span>
            </div>
            <div class="gridImgActivity">
              <div class=""></div>
              <div class="">
                <img src="<%= image_path('Icons/yet.png') %>" alt="Image" class="imgYet">
              </div>
              <div class=""></div>
            </div>
          </div>

    <% else %>
    	<% @projects.each do|project| %>
    		<div class="row mb-2">
    			<div class="col-6 position-relative">
    				<img width="100%"
    					 height="200px"
    					 class="rounded"
    					 src="<%= image_path('Skills/poly.jpg')%>" alt="poly">
               <% if project.status == 0 %>
                    <img src="<%= image_path('Icons/Idea_icon.png') %>" alt="Image" class="projectIcon in-profile-builder">
                  <% elsif project.status == 1 %>
                    <img src="<%= image_path('Icons/WIP-icon.png') %>" alt="Image" class="projectIcon in-profile-builder">
                  <% else %>
                    <img src="<%= image_path('Icons/completed-icon.png') %>" alt="Image" class="projectIcon in-profile-builder">
                <% end %>
    			</div>
    			<div class="col-6">
    				<h5 class="font-weight-bold">
    					<%= project.name %>
              <%= link_to open_edit_project_modal_path(id: project), remote: true do%>
               <i class="material-icons icons create profile-builder-edit-project-icon"  style="display:block">create</i>
              <%end%>
    				</h5>
    				<h6 class="mt-1 font-weight-bold fz-13">Description</h6>
    				<p class="fz-12"><%= project.description %></p>

    				<h6 class="mt-2 font-weight-bold fz-13">Skills</h6>
    				<p class="fz-12">
              <% project.categories.pluck(:name).each do |name| %>
              <div class="base-chips d-inline-block p-1">
                <div class="base-chips-name fz-13">
                  <%= name %>
                </div>
              </div>
              <%end%>
            </p>
    			</div>
    		</div>
    	<% end %>
    <%end%>
  </div>
</div>
